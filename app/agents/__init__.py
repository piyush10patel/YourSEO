"""Specialized SEO agents (spec §9-§10).

A team of focused agents (never a "god agent"), each returning the same
`AgentResult` contract. The `PlannerAgent` orchestrates the workers and the
`EvaluatorAgent` scores their output.
"""
